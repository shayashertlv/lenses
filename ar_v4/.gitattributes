* text=auto eol=lf

# Prepared assets are verified byte-for-byte, including their line endings.
public/models/* -text
tests/fixtures/* -text

# Public-package manifest hashes cover exact generated bytes on every host.
mobile-site/** -text
