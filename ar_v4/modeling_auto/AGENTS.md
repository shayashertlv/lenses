# Modeling Auto

This is a standalone application requested September 13, 2026. Work and write
only under this directory. Do not edit, run, stop, import code from or depend on
model_studio. It may be read only for failure lessons; do not copy its application.
Do not modify the parent application, AR, deployment, recordings or recovery files.

Read README.md, HANDOFF.md and docs/CONTRACT.md. HANDOFF.md describes the current
state only; docs/HISTORY.md holds the chronological record and docs/REVIEWS.md the
verification evidence for each change. Keep assets, dependencies, temp/cache files,
tests and receipts here. Never commit credentials or generated private assets.
Tests use fake providers and independently created synthetic Blender fixtures.
Do not send paid calls during development. Start run authorizes the selected
sequence automatically: the default standard pipeline uses two Meshy and four
Astra calls; the legacy pipeline uses two Meshy and three Astra calls. Another
edit authorizes one material/finish Astra call; the standard pipeline requires
specific written instructions, and both pipelines keep the original product notes.
Saved jobs keep their pipeline; the names "test" and "current" are accepted as
aliases for standard and legacy. No intermediate approval gates. No automatic
failed-call retry or startup resume. Keep all revisions and receipts.
Use real Blender checks and strict TypeScript. Numerical checks, including the
advisory lens-surface metrics, do not establish model quality or owner acceptance.
Record current evidence in docs/REVIEWS.md.
