========== JUDGE 0 (winner: stability-first) ==========
GRAFTS:
  - from physics-first: Replace the width-match q(u) root find with the B.4 bearing solve: sweep descent s along bridgeUp, closed-form standoff zeta*(s) = softmax interference - PAD_SINK per height, bearing B(s) = min(sideL, sideR) after standoff, pick the largest s with B(s) >= PAD_SINK - EPS_BEAR (0.08 cm), assert B monotone over the grid (else fallback), bisection-refine to 0.25 mm — keeping stability-first's event cadence, REST_TAU/REST_DEADBAND ease, confidence scaling, and degeneracy fallbacks unchanged.
    -> nose.js solveRestHeight (renamed solveRestConfiguration) in Stage 5 — the same field queries and L/C/R contact split stability-first already builds; this makes two-sided bearing solved-for instead of asserted
  - from physics-first: Per-frame non-penetration guard: after applying the eased constants, run today's seat() kernel with softmax and apply any excess over GUARD_BAND (0.03 cm) RAW that frame, counted as __ar.seat.guardPushes — penetration never waits for easing, outward motion always does.
    -> fit.js solvePlacement standoff stage / frame.js seat channel, Stage 5
  - from physics-first: mode='hold' degeneracy: when a side loses >60% of its contacts to NaN depthAt or the monotonicity assert fails, do not re-solve — freeze the applied rest constants (guard still protects) and report the mode; this is the correct hard-yaw behavior where the far sidewall is held/hallucinated.
    -> frame.js seat event scheduler, Stage 5, replacing stability-first's decay-to-optical for the occluded-sidewall case (decay stays for flat-nose/no-crossing)
  - from physics-first: First-order roll balance as the concrete law for the dark padBalance flag: phi* = clamp((I_L - I_R)/(2 * padLeverArm * kappa), +/-3 deg) with kappa read from the sweep samples, one re-evaluation of standoff after — replacing the vaguer psi-about-bridgeUp formulation.
    -> the padBalance branch of the rest solve, still behind the flag, enabled only after measurable (4) passes with it on
  - from physics-first: Exclude vertices with behind > bias (the visibility grid's own hold criterion) from fitLandmarkDepth's global sums, so the hallucinated far side stops polluting the affine's offset b at yaw — the diagnosis's scan-cause-2 fixDirection, and free since measureVisibility already runs in the same call.
    -> occluder.js fitLandmarkDepth input weighting, Stage 3 alongside the rmsNose gate (requires the measureVisibility-before-fit reorder physics-first specifies)
  - from physics-first: Solve-event triggers on person-model confidence crossings (nose-set mean conf 0.25/0.5/0.75) plus a heartbeat of at most one solve per 250 ms and at least one per 5 s — richer than commit-tick scheduling alone and bounds staleness both ways.
    -> frame.js seat event scheduler, Stage 5
  - from estimation-first: The sign-gate discipline: any channel that applies triangulated/parallax depth (the zConf crossfade onset) ships with its apply gain at 0 until a synthetic-truth check with a known +4 mm protrusion passes at BOTH yaw signs — the explicit guard against the edge-snap-class sign bug this codebase has already shipped once.
    -> Stage 4 proof gate for enabling the zConf depth crossfade in anchors.js
  - from estimation-first: Residual-vs-canonical tripwire: compute the nose-window residual against both the person estimate and the canonical baseline (~98 extra mults); if the person residual exceeds the canonical by 50% for 2 s continuously, trigger the soft decay with cause 'regression' — a structural poisoning detector, stronger than a bare residualRms threshold because it asks 'worse than the prior?' not 'large?'.
    -> person.js drift/tripwire logic, Stage 4, alongside stability-first's existing 0.25 cm residualRms rule
  - from estimation-first: Pupil-height verdict bands and seat-mode reporting: pupilHeightInLens reported with widthVerdict-style bands ('sits low' below 0.62 of target band, 'sits high' above 0.28) plus __ar.seat.mode and per-cause rebuild counters — turning the demoted optical constraint into a user-meaningful fit verdict.
    -> fit.js verdict reporting and the __ar.seat/__ar.rebuilds surfaces, Stages 5-6
  - from estimation-first: PAD_SINK' compensation: subtract half the softmax temperature from PAD_SINK (analytic bias correction) so the soft reduction's systematic under-push keeps the OCCLUDER_RELIEF >= PAD_SINK + feather inequality slack in the safe direction, instead of relying only on a one-time catalogue recalibration.
    -> nose.js softmax seat kernel constant, Stage 5
CONCERNS:
  - The seat graft must land as ONE solve: reconcile physics-first's s-parameterization (displacement along bridgeUp from the optical height, standoff eliminated in closed form) with stability-first's u-grid, REST_TAU/REST_DEADBAND channel, and confidence scaling — do not keep the width-match root find as a parallel path; keep widthAt only as the harness's independent check of the resting-height law.
  - W_PAR = 0.01 leaks the borrowed-depth prior into A_zz at 1% of accumulated weight: with W_MAX = 300 that is ~3 units of spurious depth information, below Z_CONF_MIN = 25 — but the zConf = A_zz - lambda bookkeeping must explicitly subtract the W_PAR contribution (or the derivation must bound it), or a very long frontal-only session's forgetting dynamics could drift zConf toward the crossfade threshold on prior-only information.
  - The 25th-percentile noise floor has no cap (physics-first caps at 8 cm/s / 0.25 rad/s): during sustained smooth motion with no still moments the quantile climbs and the subtraction grows — add the cap, and extend the slow-ramp harness check to a 30+ second continuous-motion scenario, not just the 0.5 Hz sweep.
  - Landmark identity slide across yaw (MediaPipe landmarks are not fixed material points) biases every ray-triangulation design, and the synthetic-truth check uses geometric rays so it cannot see this bias — before the zConf crossfade defaults on, cross-validate converged depth stability across the diag f00-f09 stills (same face, different poses) as the real-world acceptance gate.
  - Commit re-basing (viewResidual -= deltaOffsets) interacting with the shrinkage floor and the rebuild deadband needs a dedicated long-still-head harness run asserting zero net drift: a subtle pump between the 2 Hz commit cadence, SHAPE_TAU, and the per-vertex shrink floor would re-introduce exactly the breathing the design exists to kill.
  - Enumerate every existing harness check that encodes the measuring latch, estimateYaw gating, or the binary payload BEFORE Stage 2 lands (as part of Stage 0's baseline pin), so 'green modulo legitimately updated checks' is a reviewed list, not a judgment call made mid-stage.
  - The bridge filter's activity input is pose speed only: a genuine face-space bridge move on a still head (brow raise, expression) sees a closed filter and up to ~0.2 s of face-space lag — decide whether innovation magnitude should also open the cutoff (hybrid activity), and add an expression-on-still-head scenario to the pin checks.
  - The ~1540 LOC estimate is likely 1.5-2x optimistic given the offsets-to-viewResidual rename touches the hottest per-frame loop and the seat graft adds physics-first's ~220-LOC solve; Stage 0's baseline pinning contains the schedule risk but the implementer should plan stages 4-5 as the long poles.
  - Frame-one bit-equality is claimed by every stage but three separate mechanisms must each individually preserve it (empty person model, first-sample-adopted-whole pin filter, seat fallback at zero confidence) — the pin check must run per-stage, not once at the end, or a violation will be unattributable.

========== JUDGE 1 (winner: stability-first) ==========
GRAFTS:
  - from estimation-first: Replace the width-root height solve with the two-sided wedge equilibrium: minimize J(h)=d(h)^2+e_p(h)^2/W_BEAR over descent h, using per-side soft contact-patch reduction (lambda_j=exp((I_j-max)/tau_c), tau_c=0.02cm, PAD_SINK compensated by tau_c/2) and closed-form standoff s(h). Stability-first's event cadence, REST_TAU/REST_DEADBAND ease, person-confidence scaling, and degeneracy fallbacks all keep their roles — only the inner reduction changes, and its inputs (field queries, side-split contacts, bridgeUp) are already in stability-first's plumbing.
    -> nose.js solveRestHeight internals + fit.js/frame.js seatPlan (replaces q(u) root find; retires LAMBDA_PUPIL in favor of the W_BEAR soft cost)
  - from estimation-first: NoiseFloor freeze rule: admit a sample to the ring only when measuredRate < 3x current floor, cap floor rises at 10%/s, let falls be free — so a sustained sweep cannot inflate the floor into a lag source, hardening stability-first's bare 25th-percentile estimator.
    -> smoothing.js NoiseFloor class, both pose filters and the bridge-pin filter
  - from physics-first: Raw guard excess: per frame, if the softmax interference at the applied placement exceeds the eased standoff by GUARD_BAND (0.03 cm), apply the excess RAW that frame with a guardPushes counter — non-penetration never waits for REST_TAU or SEAT_TAU easing.
    -> the per-frame seat pass in fit.js solvePlacement / frame.js, alongside the existing SEAT_TAU eased-minus-raw pattern
  - from physics-first: Explicit saddle mode: if the centre band's interference exceeds both sides by 0.03 cm across the whole height sweep, the saddle is the bearing — solve height against the centre set, skip the two-sided requirement, report mode='saddle' so brow-bar/saddle-bridge assets keep today's semantics instead of failing the bearing test.
    -> the grafted equilibrium's degeneracy ladder in nose.js, using stability-first's existing centre-band contact split
  - from physics-first: Richer seat-event scheduler: trigger re-solves on person-confidence crossings (0.25/0.5/0.75), commitFit deadband crossings, and fit/model changes, with a 250 ms min-interval and 5 s heartbeat — replacing stability-first's bare <=2 Hz commit-tick coupling so the seat re-solves exactly when its inputs genuinely improved.
    -> frame.js seatPlan scheduling state
  - from estimation-first: Dual-baseline tripwire: compute the nose-window residual against BOTH the person and canonical baselines each frame (~98 extra multiplies); if the person residual exceeds the canonical residual by 50% for 2 s continuously, soft-reset — catches converged-wrong states that stability-first's absolute 0.25 cm residualRms threshold misses because a self-consistent bias keeps residuals small.
    -> person.js tripwire, replacing the absolute-threshold form
  - from estimation-first: Parallax sign harness gate: inject a +4 mm protrusion error into the synthetic stream at BOTH yaw signs and assert the recovered z-correction sign before the zConf depth crossfade is permitted to engage — the edge-snap-class sign-bug protection, run as a hard gate not a regression check.
    -> pipeline-check.js person-model block, gating the stage-4 zConf crossfade enable
CONCERNS:
  - Landmark slide bias: MediaPipe landmarks slide across the skin with pose (~0.2 mm/deg measured at the bridge); the information filter integrates this into a SELF-CONSISTENT biased depth with small residuals, invisible to both tripwires. The synthetic-truth convergence check must inject a pose-correlated slide term into the generated landmark stream, or it certifies an estimator the real detector will bias.
  - W_PAR leakage in frontal-only sessions: with zero parallax the z row still accumulates 1%-weight prior-contaminated evidence plus the ridge; prove (harness-assert) that zConf = A_zz - lambda stays below Z_CONF_MIN=25 under a long frontal-only stream, so the borrowed-depth crossfade can never engage on prior-only information.
  - The per-frame seat axis stays camera +Z: empirics seat-cause-4 (the push's meaning rotates with pose, part of the measured 2.4 mm swing) is explicitly accepted, not solved. After the grafted equilibrium lands, re-measure the pose-coupled residual on the diag replay; if it exceeds ~0.5 mm, the per-frame guard needs to act along the face normal converted at composite time.
  - Easing-chain composition: u_rest ease (REST_TAU 0.8 s) in series with SEAT_TAU + the effect deadband + the grafted raw guard gives three actors on one axis. Bound the composed step response on a solve event (no single-frame step > 0.5 mm, per measurable 4) and bound the guard's fire rate under injected noise before enabling; a guard that fires every few frames at hard poses reintroduces the step class the softmax removed.
  - Asymmetric noses with psi dark: even the grafted two-sided equilibrium cannot fully balance an asymmetric nose without roll. Add at least one asymmetric synthetic nose to measurable (1) and publish the expected bearing-gap bound for it — otherwise the check stays green on symmetric fixtures while the real complaint persists.
  - Stage-0 baseline fragility: the A/B pin assumes MediaPipe bit-determinism, which holds per-machine but not across Chrome/driver/model updates. Pin numeric baselines with tolerances rather than bit-exact snapshots, and keep the final stillness verification in the user's real Chrome (the Browser pane is rAF-throttled per the project's verification memory).
  - pipeline-check rewrite scope is underbudgeted: the 276 existing checks have heavy latch and seat-report coupling (per the 2026-08-15 review); the 320-LOC test estimate is likely 1.3-1.5x light. Enumerate the specific checks each stage rewrites during stage planning, not at discovery time.
  - Shrinkage/commit interaction: person accumulation reads raw observed points while the drawn surface uses the shrunk viewResidual, and commits re-base that residual. Assert field==mesh stays exact across a commit, and that a still head actually reaches the <=10/60 rebuild target rather than oscillating at the shrink floor (drift accumulating in sub-floor increments that still cross SURFACE_DEADBAND).

========== JUDGE 2 (winner: physics-first) ==========
GRAFTS:
  - from stability-first: Stage 0 harness-first: build diag-replay.js (fixed-seed noise over f00-f09 + face-a/b) and check in the CURRENT pipeline's baseline numbers (screen RMS per still, bridge wander, seat spread, rebuild counts) before any production change; renumber physics-first's stage 6 suite to stage 0 so every later stage's proof is a measured delta.
    -> Replaces physics-first's stage ordering: its stage-6 diag-replay content moves to a new stage 0; stages 1-5 assertions then reference pinned baselines instead of estimates.
  - from stability-first: Innovation shrinkage floor in the view-locked deform: e_tilde = e·max(0, 1−r0/|e|) with r0 = max(0.3mm, 1.5·resNoise_i), applied inside measureShape's ease so a still head's field genuinely rests — with PersonModel.update explicitly reading the RAW pre-shrinkage observed points so the estimator is never starved.
    -> physics-first C.6 (occluder.js measureShape), alongside the driftNose/driftGlobal split; tightens the rebuild target from ≤20/60 toward ≤10/60.
  - from stability-first: Per-vertex online noise estimate resNoise_i (EMA of transverse innovation, tau 2s) replacing the fixed [0.1, 0.5]cm clamp on the Huber scale r0, so the nose tip's measured 2-6x noise self-downweights with a measured per-landmark scale.
    -> physics-first A.3 step 3 (person.js update law) — sigma_i becomes the resNoise estimator and also feeds the shrinkage floor graft.
  - from estimation-first: Noise-floor freeze rule: admit ring samples only when measuredRate < 3×currentFloor, cap floor rise at 10%/s, let it fall freely — hardening the MAD floor against sustained-motion inflation; plus stability-first's slow-ramp harness assert (2cm/s tracks within 1mm).
    -> physics-first C.2 (RingMedianFloor in smoothing.js) and the stage-1 proof block.
  - from estimation-first: Residual-vs-canonical tripwire: compute the nose-window residual against BOTH the person baseline and the canonical baseline (~98 extra mults); if the person residual exceeds the canonical by 50% for 2s continuously, trigger soft decay — a sharper poisoning detector than driftScore alone because it has an absolute reference.
    -> physics-first A.7 (person.js drift alarm), running alongside driftScore.
  - from estimation-first: Softmax bias compensation: PAD_SINK' = PAD_SINK − tau/2 (or stability-first's one-time catalogue recalibration asserting ≤0.3mm standoff delta vs baseline on the canonical face) so the soft reduction's systematic downward bias does not read as a standoff regression.
    -> physics-first B.2 sideInterference and the stage-5 proof (fallback-purity check).
  - from stability-first: Seat effect deadband with hysteresis (re-arm 0.15mm, release 0.05mm) on the per-frame guard/standoff channel so the eased push literally rests between real changes instead of sub-pixel creeping — 0.026px at 45cm, above the measured 0.02-0.08mm eased-push noise.
    -> physics-first B.5 per-frame path (frame.js three-channel easing of state.seatConfig).
  - from estimation-first: Roll flag discipline: seatRoll ships dark and is enabled only after the seat-stability measurable passes WITH it on, on asymmetric synthetics at both asymmetry signs — not default-ON after generic stage validation.
    -> physics-first B.4 step 5 / stage-5 exit criteria.
  - from estimation-first: Sign-check gate pattern for the depth consumption: before the person-depth blend (A.4) carries weight, a synthetic-truth harness check injects a +4mm protrusion error at BOTH yaw signs and asserts convergence direction — the edge-snap-class bug guard, applied to triangulation consumption rather than a parallax gain.
    -> physics-first stage-3 proof block (pipeline-check.js person-model convergence checks).
  - from stability-first: Keep measuringLatch as a derived readout alias (w_meas > 0.5) instead of deleting it outright, so UI and harness consumers migrate on their own schedule.
    -> physics-first C.4 (frame.js latch removal) and the stage-2 check-rewrite list.
CONCERNS:
  - Landmark-slide bias is unmeasured by the plan's own synthetic-truth check: MediaPipe landmarks slide ~0.2mm/deg across the skin at yaw, so ray triangulation converges to a pose-averaged material point; the stage-3 synthetic uses ideal (non-sliding) landmarks and will report perfect convergence regardless. Add a slide-injected synthetic (landmark drifts with yaw at the measured rate) and decide the acceptable person-model bias bound before trusting c_i near 1.
  - s* threshold-riding: the equilibrium height is the largest s where B(s) crosses PAD_SINK − EPS_BEAR; on near-tangent wedges that crossing is shallow and s* can wander between solve events even with event gating. The solve needs adoption hysteresis or a deadband on s* itself, not just on its inputs.
  - w_pose,tri admits poses up to ~34° into the triangulation accumulator on the argument that facing×visibility already excludes hallucinated far-side landmarks there — verify that claim empirically on the diag captures (held far-side vertices at 30°+ must carry ~0 weight) before shipping, or tighten the ramp.
  - The measureVisibility reorder plus visibility-weighted and behind-excluded fit sums change fitLandmarkDepth's outputs from day one of stage 3 — enumerate which existing harness checks legitimately move, and confirm the frame-one bit-equality pin survives the reorder (visibility uses carried offsets which are zero on frame one, so it should, but it must be asserted not assumed).
  - Per-frame eigen (lambda_min) + Cramer solves over the nose set and round-robin full set: the updateFrame timing assert (≤1.2x baseline) is listed as a risk mitigation but not attached to a stage — it must land with stage 3 on a low-end profile, not at the end.
  - The pupil box prior [−1.2,+0.4]cm lets contact 'win outright' inside the box, so a bad early-session surface can hold the frame at the box edge for the first seconds despite the 0.4s ease; consider grafting estimation-first's wedge-confidence scaling of applied descent (h_applied = conf·h*) so cold sessions stay at today's optical height by construction rather than by monotonicity fallback.
  - Post-convergence event solves (250ms min interval, 5s heartbeat) are claimed to produce sub-deadband deltas 'by construction' — that is asserted without mechanism; the diag replay must specifically check for periodic micro-resettling, which would replace the current breathing complaint with a slower one.
  - Deleting measuringLatch and the binary payload rewrites long-pinned harness semantics; the stage-2 rewrite list must be enumerated file-by-file before the stage starts, and the latch alias graft adopted, or the 276-check harness churn will mask a real regression.
